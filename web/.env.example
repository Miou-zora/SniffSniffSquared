# Copy to .env.local and adjust if your Postgres differs.
#
# The web app READS the database the sniffer writes to. It never captures
# traffic itself. Start the database from the repo root with:
#     docker compose up -d
DATABASE_URL=postgres://dofus:change_me@localhost:5432/dofus
